import React from 'react';
import { SafeAreaView, View, Text, ScrollView, TouchableOpacity } from 'react-native';

const DayCruise = ({ dayCruise, navigation }: any) => {

    return (
        <SafeAreaView>
            <Text>Deň</Text>
        </SafeAreaView>
    );
}

export default DayCruise;